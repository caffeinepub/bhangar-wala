{
  "frontend/src/App.tsx": "Main app router with the `/otp-verification` route restored as a real route (no longer redirecting to `/login`), importing and rendering the OtpVerification page component alongside all other existing routes.",
  "frontend/src/pages/BookPickup.tsx": "Multi-step booking form (Address → Schedule → Items → Confirm) that passes the full selected `Address` object and scrap items array to `useCreateBooking`, shows inline error messages on the confirm step, and navigates to `/booking-confirmation` on success.",
  "frontend/src/utils/blobStorage.ts": "Blob storage utility class with fixed Uint8Array type to use `Uint8Array<ArrayBuffer>` and fixed Blob constructor to use `.buffer as ArrayBuffer` to resolve the SharedArrayBuffer type incompatibility.",
  "frontend/src/pages/Payment.tsx": "Payment screen with editable final weight inputs, itemized breakdown, Cash/UPI method selection, and pay button; fixed `bookingId` to use `useSearch` (number) instead of `BigInt` from location state, fixed `getRate` to compare `number` categoryIds, fixed `useUpdateBookingStatus` to use `bookingId` instead of `id`, and removed unused `transactionId` param.",
  "frontend/src/pages/PaymentSuccess.tsx": "Payment success screen with animated checkmark, receipt card with line items, and action buttons; fixed `bookingId` to use `useSearch` (number) instead of `BigInt` from location state, and fixed `getRate`/`getCategoryName` to use `number` categoryIds.",
  "frontend/src/pages/RateService.tsx": "Interactive 5-star rating screen with hover effects, optional comment textarea, and submit/skip buttons; fixed `bookingId` to use `useSearch` (number), changed `BigInt(rating)` to plain `rating` (number), and changed `null` comment to `undefined` to match hook signature.",
  "frontend/src/pages/Bookings.tsx": "Booking history list with status filter chips, search bar, and reverse-chronological booking cards. Fixed STATUS_CONFIG to use correct BookingStatus enum values (`partner_assigned`, `on_the_way`) and added the missing `arrived` status entry.",
  "frontend/src/pages/BookingDetails.tsx": "Detailed booking view showing address, schedule, scrap items with rates, partner info with contact buttons, and payment details. Fixed to import PaymentMethod from '../hooks/useQueries' instead of '../backend'.",
  "frontend/src/pages/Profile.tsx": "User profile screen; fixed phone display to use `profile?.phoneNumber` (the correct `UserProfile` field) instead of the non-existent `profile?.phone`.",
  "frontend/src/pages/Addresses.tsx": "Saved addresses list with edit/delete actions per card and a floating action button to add new addresses.",
  "frontend/src/pages/AddAddress.tsx": "Add address form with label selector, street/city/pincode fields, inline validation, and backend save via the fixed `useAddAddress` mutation that appends to the user profile's addresses array; shows inline error messages and navigates to `/addresses` on success.",
  "frontend/src/pages/EditAddress.tsx": "Edit address form pre-filled from backend data using route param ID, with same fields as AddAddress and updateAddress mutation on save.",
  "frontend/src/pages/Support.tsx": "Support screen with contact buttons (call/WhatsApp/email), 8-item FAQ accordion, and in-app support ticket form with subject/message fields.",
  "frontend/src/pages/Notifications.tsx": "Notifications list with unread visual distinction, mark-all-read and clear-all actions, sorted by timestamp descending with icon mapping.",
  "frontend/src/pages/BookingConfirmation.tsx": "Booking confirmation screen showing booking details, scrap items, scheduled time, and estimated earnings. Displays real assigned partner data (name, vehicle, star rating) fetched via useGetPartnerById; shows a loading skeleton while fetching, a \"Partner being assigned…\" placeholder when no partnerId exists, and a call button when partner is available.",
  "frontend/src/pages/TrackPickup.tsx": "Track pickup screen with status timeline, partner card, simulate-next-status dev button, and cancel/payment actions; fixed `bookingId` to use `useSearch` (number) instead of `BigInt` from router state, and fixed `useUpdateBookingStatus` calls to use `bookingId` instead of `id`.",
  "frontend/src/pages/Home.tsx": "Home screen with hero section, scrap category grid, rates banner, and recent bookings list. Fixed STATUS_CONFIG to use correct BookingStatus enum values (`partner_assigned`, `on_the_way`, `arrived`) and added the missing `arrived` status entry.",
  "frontend/src/pages/OtpVerification.tsx": "OTP verification screen; fixed profile existence check to call `actor.getUserProfileById(phone)` instead of the non-existent `actor.getCallerUserProfile()`.",
  "frontend/src/pages/PartnerPanel.tsx": "Partner panel page where partners enter their phone number to view assigned bookings; fixed `partnerId` prop type from `bigint` to `number`, fixed `formatDate` to accept `number` instead of `bigint`, and fixed `usePartnerUpdateBookingStatus` call to pass `{ bookingId, status, partnerId }` instead of the old `{ bookingId, partnerId }` signature.",
  "frontend/src/pages/AdminRates.tsx": "New admin scrap rate management page at `/admin/rates` showing all scrap categories with editable price-per-kg inputs; each row has a Save button that calls updateScrapRate and shows a success toast, with an info banner noting changes apply immediately.",
  "frontend/src/hooks/useQueries.ts": "All React Query hooks for backend interaction; fixed `useAddAddress` to save addresses by fetching the user profile and calling `saveCallerUserProfile` (since the backend has no standalone `addAddress` method), and fixed `useCreateBooking` to pass a proper `BookingRequest` object with `address`, `scheduledTime` (as nanosecond bigint), `items` (as `BookingItemRequest[]`), and `totalEstimatedAmount` matching the backend interface.",
  "frontend/src/index.css": "Global styles with OKLCH color system, custom animations, and a new `@media print` block that hides all elements except `.receipt-card` for clean receipt printing.",
  "frontend/index.html": "HTML entry point with PWA manifest link, theme-color meta tag (#16A34A), mobile web app meta tags, and updated apple-touch-icon reference pointing to the bhangar-wala-icon-192 generated asset.",
  "frontend/public/manifest.json": "PWA manifest for Bhangar Wala with name, short_name, theme/background colors, standalone display mode, and updated 192×192 and 512×512 icon references pointing to the bhangar-wala-icon-* generated assets.",
  "frontend/public/sw.js": "Service worker with bumped cache version `bhangar-wala-v3`, explicit early-return bypasses for Internet Identity domains (`identity.ic0.app`, `identity.internetcomputer.org`) and ICP canister API calls (`/api/v2/`) to prevent mobile \"unable to connect\" failures, plus the existing network-first/cache-first strategies for other resources.",
  "frontend/src/pages/AdminDashboard.tsx": "Admin dashboard with summary stat cards and navigation cards for all admin sections; fixed import to use `useGetPartners` instead of the non-existent `useGetAllPartners`.",
  "frontend/src/pages/AdminBookings.tsx": "Admin all-bookings list page; fixed user info display and search filter to use `userProfile.phoneNumber` instead of the non-existent `userProfile.phone`.",
  "frontend/src/pages/AdminPartners.tsx": "Admin partners management page with partner cards, add/edit form, and active toggle; fixed imports to use `useGetPartners`, `useAddPartner`, and `useUpdatePartner` (replacing non-existent `useGetAllPartners`, `useAdminAddPartner`, `useAdminUpdatePartner`, `useTogglePartnerActive`), with toggle now reusing `useUpdatePartner` passing the full partner object with flipped `active` flag.",
  "frontend/src/pages/AdminSupportTickets.tsx": "Admin support tickets page at /admin/support-tickets listing all tickets sorted by timestamp descending with filter chips (All/Open/Resolved), showing subject, message, timestamp, and status. Each ticket has a Switch to toggle between open/resolved status via the backend.",
  "frontend/src/pages/ShopRegister.tsx": "Multilingual scrap shop registration form supporting 5 languages; fixed `scrapCategoriesHandled` to pass plain `number[]` (via `Array.from(selectedCategories)`) instead of `bigint[]`.",
  "frontend/src/pages/ShopRegisterStatus.tsx": "Registration status page at /shop-register/status that fetches the shop record by phone from localStorage, displays shop info card with status badge (amber/pending, green/approved, red/rejected), translated status notes in 5 languages via a language selector, and contextual action buttons (Go to Home for approved, Re-register for rejected, Register Now if no shop found).",
  "frontend/src/pages/AdminShopRegistrations.tsx": "Admin page at /admin/shop-registrations that fetches all scrap shops, displays them as cards with full details (shop name, owner, phone, city, area, pincode, categories, language, date, status badge), filter chips (All/Pending/Approved/Rejected), search bar, and Approve/Reject action buttons for pending entries with inline loading states.",
  "frontend/src/pages/Login.tsx": "Login screen with phone number input, country code selector defaulting to +91, a required terms & conditions checkbox, and a \"Send OTP\" button that stores the phone in sessionStorage and navigates to /otp-verification. No Internet Identity UI.",
  "frontend/src/pages/ProfileSetup.tsx": "Profile setup form that reads the phone number from localStorage auth session, calls `actor.createUserProfile(userId, name, address)` directly with the phone as userId, handles all result variants (`#ok`, `#alreadyExists`, `#invalidId`), passes null for address when fields are empty or a proper Address object when filled, persists the auth session on success, and navigates to /home.",
  "frontend/src/pages/Splash.tsx": "Splash screen that checks localStorage for a valid auth session (no Internet Identity dependency) and redirects authenticated users to /home or unauthenticated users to /login after a 2.2-second delay."
}